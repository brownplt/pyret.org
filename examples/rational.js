export const example = `use context starter2024`;

export const rational = {
    definitionsAtLastRun: example,
    interactionsSinceLastRun: [
        "# Click rationals to see their decimal representation\n1 / 8",
        "# Compare to 0.6000000000000001 in floating point\n0.1 + 0.2 + 0.3",
        "# False using floating point!\n(0.1 + (0.2 + 0.3)) == (0.1 + 0.2 + 0.3)",
        "# Numbers are shown in reduced form\n(22 * 2) / (13 + 1)",
        "# Truly approximate results are explicitly labelled with ~\nsqrt(2)"
    ],
    editorContents: example,
    replContents: ""
}
